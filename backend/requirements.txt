# Backend EmotionLens — dependências de execução.
# Alvo: Python 3.10 ou 3.11.
# Instale em uma virtualenv dedicada (veja README.md).

# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Visão computacional ---
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.0.0

# --- Pipeline de emoções (HSEmotion + YOLOv8-face) ---
# torch/torchvision: por padrão instalam a build de CPU. Para GPU (CUDA),
# instale a partir do índice oficial do PyTorch (ver README) — o código detecta
# e usa a GPU automaticamente quando disponível.
torch==2.5.1
torchvision==0.20.1
timm==1.0.11
ultralytics==8.3.49
hsemotion==0.3.0
